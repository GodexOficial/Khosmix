import { Quicksand, Oxanium } from "next/font/google";

export const quicksand = Quicksand({
  subsets: ["latin"],
  weight: ["300", "400", "500", "700"],
});

export const oxanium = Oxanium({
  subsets: ["latin"],
  weight: ["300", "400", "500", "700"],
});
